# Postgres connection string. On Render, use the *Internal* Database URL from
# your database's dashboard page when the app runs in the same region.
DATABASE_URL=postgresql://user:password@host/dbname
